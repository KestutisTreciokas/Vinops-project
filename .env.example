# ---------- Public site ----------
NEXT_PUBLIC_SITE_URL=https://vinops.online

# ---------- Feature flags / ops ----------
# Страницы VIN, которые должны открываться даже при отсутствии данных (через запятую)
TEST_VINS=WAUZZZAAAAAAAAAAA

# Метаданные сборки (попадают в /api/v1/health)
APP_VERSION=dev
GIT_SHA=local

# ---------- Database ----------
# DATABASE_URL=postgres://user:pass@host:5432/dbname
